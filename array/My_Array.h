#ifndef MY_ARRAY_H
#define MY_ARRAY_H
//dynamic array -- vector in C++
class MyArray
{
public:
    MyArray(int capacity);
    ~MyArray();
    int size();
    int capacity();
    bool is_empty();

private:

};

#endif